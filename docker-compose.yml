version: '3.8'
services:
  db:
    image: postgres:15
    container_name: sentinel-db
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=sentinel_db
    ports:
      - "5432:5432"